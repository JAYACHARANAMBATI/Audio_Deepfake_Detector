#create env for better usage of the project
(ones){
    create--> python -m venv env


Activate it
         On Mac / Linux-->source env/bin/activate

         On Windows-->env\Scripts\activate


now install the requirements
        ---> pip install -r requirements.txt
}




finally (when you want to run the project) use the following command in the terminal
------>running command-->streamlit run streamlit_app.py